const superHeros: string[] = []

superHeros.push("spiderman")

type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = []

allUsers.push({name: "Pravesh", isActive: true})

const MLModels: number[][] = [
    [255, 255, 255]
]

export {}